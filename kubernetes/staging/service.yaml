apiVersion: v1
kind: Service
metadata:
  name: shopeasy-service
  namespace: staging
spec:
  selector:
    app: shopeasy
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080